# Tooth Project

## 数据预览

共75个项目，每个项目



## 工作记录

将stl文件转为pcd文件

stl文件转为png文件可视化(`src/save_stl_into_png.py`)

pointNet训练

目前3D模型只能用点云图片来处理


将dcm文件转为点云文件
    
    首先将dcm文件使用Windows软件转为stl文件
    然后将stl文件转为pcd文件
